{
  "name": "lia-stack",
  "version": "1.0.1",
  "description": "JavaScript library of Execute multiple asynchronous tasks and return synchronous results",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest --config jestconfig.json",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "build": "tsc",
    "install": "yarn",
    "prepare": "npm run build",
    "prepublishOnly": "npm run test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src"
  },
  "author": "sunlley",
  "license": "ISC",
  "files": [
    "dist/**/*",
    "dist/*",
    "dist"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "@types/node": "^18.17.1",
    "jest": "^29.6.2",
    "prettier": "^2.8.8",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
  }
}
